# V8 Core
rich>=13.0.0
pypdf>=3.0.0
python-dotenv==1.1.1
typer==0.8.0
textual>=0.40.0
PyYAML>=6.0

# V9 Cognition — Phase 1: The Brain
requests>=2.31.0       # bridge.py: HTTP calls to Ollama
lancedb>=0.6.0         # memory.py: local vector store
pyarrow>=14.0.0        # memory.py: LanceDB schema dependency

# V9 Cognition — Phase 2: The Inspector
pdfplumber>=0.10.0     # inspector.py + listener.py: reliable PDF text extraction

# V9 Cognition — Phase 4: The Listener
watchdog>=3.0.0        # listener.py: cross-platform filesystem event watcher

# V9 Cognition — Phase 5: The Eye
Pillow>=10.0.0         # eye.py: image reading and preprocessing
imagehash>=4.3.0       # eye.py: perceptual hashing for visual dedup
pytesseract>=0.3.10    # eye.py: OCR for scanned documents (requires Tesseract binary)

# V10 Colleague Mode — The UI
fastapi>=0.100.0
uvicorn>=0.22.0
pywebview>=4.2.0
jinja2>=3.1.0          # HTML templating
python-multipart>=0.0.6 # File uploads for UI
